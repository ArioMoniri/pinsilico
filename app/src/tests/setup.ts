// Vitest setup. Phase 7+ adds MSW handlers; Phase 0 just installs
// @testing-library/jest-dom matchers so test files can `expect(...).toBeInTheDocument()`.
import "@testing-library/jest-dom/vitest";
